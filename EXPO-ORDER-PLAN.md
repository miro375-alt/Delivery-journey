# 박람회 택배 주문 접수 시스템 — 기획서 (v2 · 결정사항 반영)

> 박람회 현장에서 택배 배송을 원하는 고객의 주문자 정보를 태블릿(아이패드)으로 수집하고,
> 구글시트에 자동 정리하여 내부 발송요청까지 이어지는 시스템의 기획 문서입니다.

## 확정된 결정사항 (v2)

| 항목 | 결정 |
|------|------|
| 제품 정보 | 폼에서 수집하지 않음 — **영수증(주문)번호로 키오스크 주문내역과 내부 매칭** |
| 배송비 | **3만원 이상 무료 / 3만원 미만 선결제** — 폼에서 금액 구분 선택 + 스태프 결제확인 체크 |
| 발송의뢰 포맷 | 미정 → 범용 CSV 내보내기로 대응, 확정 시 컬럼 매핑만 조정 |
| 태블릿 | **아이패드** — Safari 홈화면 웹앱 + 사용법 유도(Guided Access) 모드 |
| 접수 알림 | **필요** — Apps Script `MailApp`으로 접수 건당 담당자 메일 발송 |

---

## 1. 배경과 목표

### 현장 상황
1. 고객이 **키오스크에서 제품 결제** → 영수증 + 교환증 수령
2. 교환증을 들고 **카운터에서 제품 수령** (현장 수령 고객)
3. **택배 배송을 원하는 고객** → 옆 태블릿에서 주문자 정보 입력 ← **이번 작업 범위**
4. 수집된 정보가 **구글시트에 자동 적재** → 내부 담당자가 발송요청 처리

### 목표
| 목표 | 측정 기준 |
|------|-----------|
| 빠른 접수 | 고객 1명당 입력 시간 **90초 이내** (제품 선택 제거로 단축) |
| 데이터 정확성 | 주소 오류 0건 (우편번호 API 강제) |
| 유실 방지 | 현장 와이파이 끊겨도 **접수 데이터 유실 0건** |
| 운영 부담 최소화 | 별도 서버 없이 운영 가능 |

---

## 2. 사용자 흐름 (User Flow)

```
[고객]                          [태블릿]                       [구글시트]
결제 완료, 영수증 소지
   │
   ├─ 스태프 안내: "택배는 이쪽에서 접수해주세요"
   │
   ▼
① 시작 화면 터치 ──────────▶ 접수 시작
② 영수증 번호 입력(또는 스태프가 입력)
③ 주문금액 구분 선택 (3만원 이상/미만)
   └ 미만이면: 배송비 선결제 → 스태프가 결제확인 체크
④ 이름·연락처 입력 (수령인이 다르면 토글로 추가 입력)
⑤ 우편번호 검색 → 주소 자동입력 + 상세주소
⑥ 배송 요청사항(선택)
⑦ 개인정보 수집·이용 동의 체크
⑧ [접수하기] ──────────────▶ 전송 ─────────────▶ 새 행 추가
                                │                    + 담당자 메일 알림
⑨ 완료 화면(접수번호 표시) ◀──┘
   │ 10초 후 자동으로 ①로 복귀
   ▼
영수증에 스태프가 접수번호 기재 → 교환증은 회수(중복 수령 방지)
```

### 운영 모드 (고객 직접 입력 + 스태프 보조)
- **고객이 직접 입력**: 이름/연락처/주소 오타 책임이 고객에게 있고, 스태프는 병렬로 다른 응대 가능
- **스태프 필수 개입 지점**: 영수증 번호 확인, 3만원 미만 건 배송비 수취 + 결제확인 체크, 교환증 회수
- 아이패드는 **사용법 유도(Guided Access)** 모드로 잠가 다른 앱 이탈 방지

---

## 3. 수집 항목 정의

| # | 항목 | 필수 | 입력 방식 | 검증 |
|---|------|:---:|-----------|------|
| 1 | 영수증(주문) 번호 | ✅ | 직접 입력 | 중복 접수 시 시트에 "중복의심" 플래그 |
| 2 | 주문금액 구분 | ✅ | 버튼 선택: `3만원 이상(무료)` / `3만원 미만(배송비)` | — |
| 3 | 배송비 결제확인 | 조건부 | 체크박스 (**스태프 확인**) | 3만원 미만 선택 시 필수 |
| 4 | 주문자 이름 | ✅ | 텍스트 | 2자 이상 |
| 5 | 휴대폰 번호 | ✅ | 숫자 키패드 | 자동 하이픈, 패턴 검증 |
| 6 | 수령인 이름/연락처 | 조건부 | "받는 분이 달라요" 토글 시 노출 | 기본은 주문자와 동일 |
| 7 | 우편번호/기본주소 | ✅ | **다음(카카오) 우편번호 검색 API** | 검색 결과만 허용 (오프라인 시 수동 입력 폴백) |
| 8 | 상세주소 | ✅ | 텍스트 | 1자 이상 |
| 9 | 배송 요청사항 | — | 자주 쓰는 문구 버튼(문 앞, 경비실 등) + 직접입력 | — |
| 10 | 개인정보 동의 | ✅ | 체크박스 + 전문 보기 | 미체크 시 제출 불가 |
| 11 | 접수 시각 | 자동 | 시스템 | — |
| 12 | 접수번호 | 자동 | `E{월일}-{단말기}-{일련번호}` 예: `E0912-T1-047` | 완료 화면에 크게 표시 |

> **제품명/수량은 수집하지 않음** — 영수증 번호로 키오스크 주문내역과 내부 매칭.
> 발송 검수 단계에서 키오스크 정산 데이터를 시트에 붙여넣고 VLOOKUP 매칭(또는 수동 확인).

---

## 4. 시스템 아키텍처

### Google Apps Script 웹앱 (서버리스)

```
[아이패드 Safari]                     [Google]
 expo/order.html (정적 페이지)
   │  fetch POST (JSON, text/plain)
   ▼
 Apps Script 웹앱 (doPost) ────────▶ 구글시트 "주문접수" 탭에 행 추가
   │                                   │
   └─ 응답: ok                          └─ MailApp → 담당자 메일 알림 (접수 건당)
```

**선택 이유**: 시트 네이티브 연동(인증 불필요), 서버 운영 불필요, 무료, 반나절 구축.
NAS/PHP 자산은 1회성 행사에는 과함 — 백업 경로로만 유지.

### 접수번호 — 클라이언트 발급 (오프라인 대응 핵심)
- 서버 발급이면 오프라인 시 완료 화면에 번호를 못 보여줌 →
  **단말기 코드 + 일자별 로컬 일련번호**로 클라이언트에서 발급 (`E0912-T1-047`)
- 단말기 코드(T1, T2…)는 스태프 화면에서 설정 → 다중 태블릿에서도 충돌 없음

### 오프라인 대비
1. 제출 시 **localStorage 큐에 먼저 저장** → 전송 성공 시 제거 (전송 실패해도 고객에게는 정상 완료 화면)
2. 30초 간격 + 네트워크 복구 이벤트 시 자동 재전송
3. 화면 상단에 미전송 건수 배지(스태프용 작은 표시)
4. 스태프 화면(로고 5초 롱프레스)에서 큐 상태 확인 / 수동 재전송 / **CSV 백업 다운로드**(최후 수단)

### 중복 제출 방지
- 프런트: 제출 버튼 즉시 비활성화 + 접수마다 클라이언트 UUID 부여
- 백엔드: 동일 UUID 재수신 시 행 추가 없이 성공 응답(멱등) — 재전송 큐와 충돌 없음
- 영수증 번호 중복은 차단하지 않고 시트에 **"중복의심" 플래그** (분할 접수 등 정상 케이스 존재)

### 알림
- Apps Script `MailApp.sendEmail` — 접수 건당 담당자 메일 (수신자는 Code.gs 상단에서 설정, 복수 가능)
- 메일 실패가 접수 저장을 막지 않도록 try/catch 분리

---

## 5. 구글시트 설계

### 탭 구성
| 탭 | 용도 | 작성 주체 |
|----|------|-----------|
| `주문접수` | 태블릿 접수 원본 (Apps Script가 행 추가) — **수정 금지** | 시스템 |
| `발송처리` | 내부 처리 현황 — 상태/제품매칭/송장 관리 | 담당자 |
| `키오스크주문` | 키오스크 정산 데이터 붙여넣기 → 제품 매칭 소스 | 담당자 |

### `주문접수` 탭 컬럼
```
A 접수번호      B 접수시각        C 영수증번호     D 주문금액구분   E 배송비결제확인
F 주문자명      G 주문자연락처    H 수령인명       I 수령인연락처   J 우편번호
K 기본주소      L 상세주소        M 배송요청사항   N 개인정보동의   O 클라이언트UUID
P 단말기        Q 중복의심
```

### `발송처리` 탭 상태값 (드롭다운 + 조건부 서식)
```
접수 → 검수완료 → 발송요청 → 송장등록 → 배송완료
                └→ 보류(사유 기입) / 취소
```
추가 컬럼: `상태`, `제품명(매칭)`, `수량`, `담당자`, `택배사`, `송장번호`, `발송일`, `비고`

### 내부 발송요청 흐름
1. 행사 당일 저녁/익일 오전, 담당자가 `키오스크주문` 탭에 정산 데이터 붙여넣기 → 영수증 번호로 제품 매칭
2. `발송처리`에서 검수(주소/연락처/배송비 수취 여부) → **발송요청** 상태로 변경
3. 발송요청 건을 CSV로 내보내 물류 전달 (포맷 확정 시 컬럼 매핑 조정)
4. 송장번호 입력 → 고객 문자 안내(수동, 추후 자동화 여지)

---

## 6. 화면 설계

### ① 시작 화면
- 큰 버튼: **"택배 배송 접수 시작"** / 하단에 행사명, 미전송 배지(스태프용)

### ② 접수 폼 — 단일 스크롤 폼
```
[ 영수증 번호 ]
[ 3만원 이상 (무료배송) ] [ 3만원 미만 (배송비 3,500원) ]
  └ 미만 선택 시: ☑ 배송비 선결제 확인 (스태프 확인)
[ 이름 ]  [ 휴대폰 번호 ]
( ) 받는 분이 달라요 → 수령인 이름/연락처 토글
[ 우편번호 검색 🔍 ] → 카카오 주소검색 레이어 (오프라인 시 수동입력 폴백)
[ 상세주소 ]
[ 요청사항: 문앞 | 경비실 | 직접입력 ]
☑ 개인정보 수집·이용 동의 (전문 보기)
[        접 수 하 기        ]   ← 하단 고정, 필수값 충족 시 활성화
```

### ③ 완료 화면
- ✅ "접수가 완료되었습니다" + **접수번호 크게 표시** → 스태프가 영수증에 기재
- 안내: "영업일 기준 2~3일 내 발송됩니다" / 10초 후 자동 초기화 복귀

### ④ 스태프 화면 (로고 5초 롱프레스)
- 오늘 접수 건수 / 미전송 큐 / 수동 재전송 / CSV 백업 / 단말기 코드·웹앱 URL 설정

---

## 7. 개인정보 처리

- 수집 항목: 이름, 연락처, 주소 (배송 목적 한정)
- 동의 문구: "수집된 개인정보는 상품 배송 목적으로만 이용되며, 배송 완료 후 3개월 이내 파기됩니다."
- 시트 접근: 담당자 계정만 편집, 링크 공유 금지
- 완료 화면 복귀 시 폼 데이터 완전 초기화 (이전 고객 정보 노출 방지)
- 배송 완료 후 로컬(localStorage) 백업 데이터도 스태프 화면에서 삭제

---

## 8. 산출물

```
expo/
├── order.html              # 아이패드 접수 페이지 (단일 파일: HTML+CSS+JS)
├── apps-script/Code.gs     # doPost(저장·멱등), 메일 알림, doGet(헬스체크)
└── SETUP.md                # 시트 생성 → Apps Script 배포 → 아이패드 세팅 → 리허설 절차
```

### 행사 전 체크리스트 (상세는 SETUP.md)
- [ ] Apps Script 배포 → 스태프 화면에서 웹앱 URL 설정 → 실전 전송 테스트 10건
- [ ] 담당자 메일 수신 확인
- [ ] 아이패드: 홈화면 추가(전체화면), 자동잠금 해제, Guided Access, 충전기 상시 연결
- [ ] 와이파이 끊고 접수 → 복구 후 자동 전송 확인
- [ ] 스태프 교육: 접수번호 영수증 기재, 배송비 수취·확인 체크, 교환증 회수, 스태프 화면 진입법
- [ ] 예비 아이패드 1대 (URL만 열면 즉시 대체, 단말기 코드 T2로 설정)
