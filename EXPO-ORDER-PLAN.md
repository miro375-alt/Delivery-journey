# 박람회 택배 주문 접수 시스템 — 기획서

> 박람회 현장에서 택배 배송을 원하는 고객의 주문자 정보를 태블릿으로 수집하고,
> 구글시트에 자동 정리하여 내부 발송요청까지 이어지는 시스템의 기획 문서입니다.

---

## 1. 배경과 목표

### 현장 상황
1. 고객이 **키오스크에서 제품 결제** → 영수증 + 교환증 수령
2. 교환증을 들고 **카운터에서 제품 수령** (현장 수령 고객)
3. **택배 배송을 원하는 고객** → 옆 태블릿에서 주문자 정보 입력 ← **이번 작업 범위**
4. 수집된 정보가 **구글시트에 자동 적재** → 내부 담당자가 발송요청 처리

### 목표
| 목표 | 측정 기준 |
|------|-----------|
| 빠른 접수 | 고객 1명당 입력 시간 **90초 이내** |
| 데이터 정확성 | 주소 오류 0건 (우편번호 API 강제) |
| 유실 방지 | 현장 와이파이 끊겨도 **접수 데이터 유실 0건** |
| 운영 부담 최소화 | 별도 서버 없이 운영 가능 |

---

## 2. 사용자 흐름 (User Flow)

```
[고객]                          [태블릿]                       [구글시트]
결제 완료, 영수증 소지
   │
   ├─ 스태프 안내: "택배는 이쪽에서 접수해주세요"
   │
   ▼
① 시작 화면 터치 ──────────▶ 접수 시작
② 영수증 번호 입력(또는 스태프가 입력)
③ 제품/수량 선택
④ 이름·연락처 입력
⑤ 우편번호 검색 → 주소 자동입력 + 상세주소
⑥ 배송 요청사항(선택)
⑦ 개인정보 수집·이용 동의 체크
⑧ [접수하기] ──────────────▶ 전송 ─────────────▶ 새 행 추가
                                │                    (상태: 접수)
⑨ 완료 화면(접수번호 표시) ◀──┘
   │ 10초 후 자동으로 ①로 복귀
   ▼
영수증에 스태프가 "택배접수 완료" 도장/표시 → 교환증은 회수(중복 수령 방지)
```

### 운영 모드 결정 (권장: 고객 직접 입력 + 스태프 보조)
- **고객이 직접 입력**: 이름/연락처/주소 오타 책임이 고객에게 있고, 스태프는 병렬로 다른 응대 가능
- **스태프 필수 개입 지점**: 영수증 번호 확인, 교환증 회수, 접수 완료 확인
- 태블릿은 **키오스크 모드**(iOS 사용법 유도 접근 / Android 고정 모드)로 잠가 다른 앱 이탈 방지

---

## 3. 수집 항목 정의

| # | 항목 | 필수 | 입력 방식 | 검증 |
|---|------|:---:|-----------|------|
| 1 | 영수증(주문) 번호 | ✅ | 직접 입력 | 숫자, 중복 접수 시 경고 |
| 2 | 제품명 | ✅ | 버튼 선택(사전 등록 목록) | — |
| 3 | 수량 | ✅ | +/- 스테퍼 | 1~99 |
| 4 | 주문자 이름 | ✅ | 텍스트 | 2자 이상 |
| 5 | 휴대폰 번호 | ✅ | 숫자 키패드 | 010-XXXX-XXXX 자동 하이픈, 패턴 검증 |
| 6 | 우편번호/기본주소 | ✅ | **다음(카카오) 우편번호 검색 API** | 검색 결과만 허용(직접 입력 불가) |
| 7 | 상세주소 | ✅ | 텍스트 | 1자 이상 |
| 8 | 배송 요청사항 | — | 텍스트 or 자주 쓰는 문구 버튼(문 앞, 경비실 등) | — |
| 9 | 개인정보 동의 | ✅ | 체크박스 + 전문 보기 | 미체크 시 제출 불가 |
| 10 | 접수 시각 | 자동 | 시스템 | — |
| 11 | 접수번호 | 자동 | `E{날짜}-{일련번호}` 예: `E0912-047` | 완료 화면에 크게 표시 |

> **받는 사람 ≠ 주문자** 케이스: "받는 분이 다른가요?" 토글 → 켜면 수령인 이름/연락처 필드 추가 노출 (기본은 주문자와 동일하게 자동 복사)

---

## 4. 시스템 아키텍처

### 권장안: Google Apps Script 웹앱 (서버리스)

```
[태블릿 브라우저]                     [Google]
 order.html (정적 페이지)
   │  fetch POST (JSON)
   ▼
 Apps Script 웹앱 (doPost) ────────▶ 구글시트 "주문접수" 탭에 행 추가
   │                                   │
   └─ 응답: 접수번호                    └─ (선택) 새 접수 시 담당자에게 메일/카톡 알림
```

**선택 이유**
| 기준 | Apps Script 웹앱 (권장) | NAS PHP 백엔드 (기존 자산 활용) |
|------|------------------------|--------------------------------|
| 시트 연동 | 네이티브 (`SpreadsheetApp`) — 인증 불필요 | Google Sheets API + 서비스계정 키 관리 필요 |
| 서버 운영 | 불필요 (구글 인프라) | NAS 상시 가동 + 외부 접근 설정 필요 |
| 현장 장애 리스크 | 구글 가용성에만 의존 | NAS·회사망·DDNS 등 장애 지점 다수 |
| 비용 | 무료 | NAS 운영 |
| 구축 속도 | 반나절 | 1~2일 |

> 기존 저장소의 PHP/NAS 배포 자산은 **백업 경로**로 남겨둘 수 있으나, 1회성 박람회 이벤트에는 Apps Script가 압도적으로 단순하고 안전.

### 프런트엔드
- **순수 HTML/CSS/JS 단일 페이지** (`expo/order.html`) — 기존 저장소 스타일과 동일하게 빌드 불필요
- 태블릿 가로/세로 대응, 터치 타겟 최소 48px
- 다음 우편번호 API(무료, 키 불필요) embed 방식

### 오프라인 대비 (핵심 요구)
박람회장 와이파이는 불안정하다는 전제로 설계:
1. 제출 시 전송 실패 → **localStorage 큐에 저장** 후 고객에게는 정상 완료 화면 표시
2. 백그라운드에서 30초 간격 재전송 시도, 성공 시 큐에서 제거
3. 화면 상단에 **미전송 건수 배지** 표시 (스태프만 인지 가능한 작은 표시)
4. 행사 종료 시 스태프 화면(롱프레스 5초로 진입)에서 큐 상태 확인 + 수동 재전송 + CSV 내보내기(최후 수단)

### 중복 제출 방지
- 프런트: 제출 버튼 즉시 비활성화 + 클라이언트 UUID를 요청에 포함
- 백엔드(Apps Script): 동일 UUID 재수신 시 기존 접수번호 반환(멱등 처리) — 재전송 큐와 충돌 없음
- 영수증 번호 중복은 **차단하지 않고 시트에 "중복의심" 플래그** (분할 배송 등 정상 케이스 존재)

---

## 5. 구글시트 설계

### 탭 구성
| 탭 | 용도 | 작성 주체 |
|----|------|-----------|
| `주문접수` | 태블릿 접수 원본 (Apps Script가 행 추가) | 시스템 |
| `발송처리` | 내부 처리 현황 — `주문접수`를 참조하고 상태/송장 컬럼 추가 | 담당자 |
| `제품목록` | 태블릿 선택지 소스 (제품명/코드) | 관리자 |
| `설정` | 접수번호 시퀀스, 행사 정보 | 시스템 |

> 원본(`주문접수`)은 **수정 금지 원칙** — 정정이 필요하면 `발송처리` 탭에서. 원본 보존으로 분쟁 대응.

### `주문접수` 탭 컬럼
```
A 접수번호      B 접수시각        C 영수증번호    D 제품명       E 수량
F 주문자명      G 주문자연락처    H 수령인명      I 수령인연락처  J 우편번호
K 기본주소      L 상세주소        M 배송요청사항  N 개인정보동의  O 클라이언트UUID
P 중복의심
```

### `발송처리` 탭 상태값 (드롭다운 + 조건부 서식 색상)
```
접수 → 검수완료 → 발송요청 → 송장등록 → 배송완료
                └→ 보류(사유 기입) / 취소
```
추가 컬럼: `상태`, `담당자`, `택배사`, `송장번호`, `발송일`, `비고`

### 내부 발송요청 흐름
1. 행사 당일 저녁 또는 익일 오전, 담당자가 `발송처리` 탭에서 **검수** (주소/연락처 이상 여부)
2. 검수완료 건을 **발송요청** 상태로 변경 → 물류 담당에게 전달
   - 옵션 A: 시트 공유로 물류 담당이 직접 확인
   - 옵션 B: Apps Script로 "발송요청" 상태 건만 **CSV/발송의뢰 양식으로 내보내기** (사방넷 업로드 양식 등 — 기존 `.env`에 사방넷 키가 있으므로 추후 자동화 여지)
3. 송장번호 입력 → 고객 문자 안내(수동 or 추후 자동화)

---

## 6. 화면 설계 (와이어프레임 수준)

### ① 시작 화면
- 큰 버튼 하나: **"택배 배송 접수 시작"**
- 하단 작게: 행사명, 미전송 건수 배지(스태프용)

### ② 접수 폼 — 한 화면 스크롤형 (단계 분할 대신)
> 90초 목표 달성을 위해 스텝 위저드가 아닌 **단일 스크롤 폼** + 섹션 구분. 항목이 9개뿐이라 스텝 전환 오버헤드가 더 큼.

```
[ 영수증 번호 ]                    ← 숫자 키패드
[ 제품 선택 버튼들 ] [ 수량 - 1 + ]
[ 이름 ]  [ 휴대폰 번호 ]
( ) 받는 분이 달라요 → 수령인 필드 토글
[ 우편번호 검색 🔍 ] → 카카오 주소검색 레이어
[ 상세주소 ]
[ 요청사항: 문앞 | 경비실 | 직접입력 ]
☑ 개인정보 수집·이용 동의 (보기)
[        접 수 하 기        ]      ← 하단 고정, 필수값 충족 시 활성화
```

### ③ 완료 화면
- ✅ "접수가 완료되었습니다"
- **접수번호 크게 표시** (예: E0912-047) — 스태프가 영수증에 옮겨 적음
- 안내 문구: "영업일 기준 2~3일 내 발송됩니다"
- 10초 카운트다운 후 자동으로 시작 화면 복귀 (또는 "처음으로" 버튼)

### ④ 스태프 화면 (숨김 — 로고 5초 롱프레스)
- 오늘 접수 건수 / 미전송 큐 상태 / 수동 재전송 / CSV 백업 다운로드 / 접수 테스트 모드

---

## 7. 개인정보 처리

- **수집 항목**: 이름, 연락처, 주소 (배송 목적 한정)
- **동의 문구 예시**: "수집된 개인정보는 상품 배송 목적으로만 이용되며, 배송 완료 후 3개월 이내 파기됩니다."
- 시트 접근 권한: 담당자 계정만 편집 가능, 링크 공유 금지
- 행사 종료 + 배송 완료 후 시트의 개인정보 컬럼 파기(또는 시트 삭제) 일정을 `설정` 탭에 기록
- 태블릿 완료 화면 복귀 시 **폼 데이터 완전 초기화** (이전 고객 정보 노출 방지)

---

## 8. 구현 계획

### 산출물 파일 구조 (이 저장소 내)
```
expo/
├── order.html          # 태블릿 접수 페이지 (단일 파일: HTML+CSS+JS)
├── apps-script/
│   └── Code.gs         # doPost(접수 저장·멱등 처리), 접수번호 발급, 알림(선택)
├── SETUP.md            # 구글시트 생성 → Apps Script 배포 → 태블릿 세팅 절차
└── sheet-template.md   # 시트 탭/컬럼/드롭다운/조건부서식 세팅 가이드
```

### 단계별 일정 (총 2~3일 작업)
| 단계 | 내용 | 소요 |
|------|------|------|
| 1 | 구글시트 템플릿 + Apps Script `doPost` (행 추가, 접수번호, 멱등) | 0.5일 |
| 2 | 접수 페이지 UI (폼, 검증, 우편번호 API, 자동 복귀) | 1일 |
| 3 | 오프라인 큐 + 재전송 + 스태프 화면 | 0.5일 |
| 4 | 태블릿 실기기 테스트(가로/세로, 키보드 가림, 키오스크 모드) + 리허설 | 0.5일 |

### 행사 전 체크리스트
- [ ] Apps Script 웹앱 배포 URL을 `order.html`에 설정, 실제 시트로 전송 테스트 10건
- [ ] 태블릿 브라우저 전체화면 + 키오스크 모드 + 절전 해제 + 충전기 상시 연결
- [ ] 와이파이 끊은 상태로 접수 → 복구 후 자동 전송 확인
- [ ] 스태프 교육: 접수번호 → 영수증 기재, 교환증 회수 규칙, 스태프 화면 진입법
- [ ] 예비 태블릿 1대 (동일 페이지 URL만 열면 즉시 대체 가능)

---

## 9. 결정 필요 사항 (Open Questions)

1. **제품 목록**: 판매 제품 종류와 명칭 확정 필요 (버튼 선택지로 하드코딩 예정)
2. **알림 방식**: 새 접수 시 담당자 알림이 필요한지 (메일 / 없음 — 시트 실시간 확인으로 충분한지)
3. **배송비**: 키오스크 결제에 포함인지, 현장 별도 수취인지 → 별도 수취라면 "배송비 결제확인" 체크 항목 추가
4. **발송의뢰 양식**: 물류 쪽에 전달하는 포맷(사방넷 업로드 양식 등)이 정해져 있으면 CSV 내보내기 컬럼을 거기에 맞춤
5. **태블릿 기종**: iOS/Android 확정 시 키오스크 모드 세팅 가이드 구체화

---

*본 기획 확정 후 `expo/` 디렉터리에 구현을 진행합니다.*
