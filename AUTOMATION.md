# ⚙️ 자동화 & 데이터 연동 가이드

대상: 자사몰 **아가드(aguard)** · **베이비스탠다드(babystandard)** (카페24 + 사방넷)
목표: 추천 / 인스타 피드 / 진행 행사 를 **최대한 자동화**.

> 핵심 원칙: 현재는 정적 사이트(GitHub Pages)입니다. API 키는 프론트에 절대 노출하면 안 되므로,
> 자동화는 **GitHub Actions(cron) 또는 얇은 서버리스**가 API를 호출해 `data/*.json`을 생성하고,
> 프론트는 그 JSON만 읽는 구조를 권장합니다.

---

## 1. 추천 데이터 (엑셀 → 카탈로그)

추천 엔진은 이미 구현되어 있고, **데이터만 넣으면** 동작합니다.
점수식: `같은 연령대(+3) / 인접 연령대(+1)` + `같은 브랜드(+2)` + `같은 몰(+1)` + `같은 카테고리(+1)` → 상위 2개.

### 엑셀 컬럼(권장)
| 컬럼 | 필수 | 예시 | 설명 |
|---|:--:|---|---|
| `id` | ✅ | ag-1001 | 상품 고유코드(중복X) |
| `brand` | ✅ | aguard / babystandard | 브랜드 키 |
| `mall` | ✅ | aguard / babystandard | 구매(판매) 몰 |
| `ageGroup` | ✅ | 6-12개월 | **연령대(추천 핵심)** |
| `category` | ✅ | 안전용품 | 카테고리 |
| `name` | ✅ | 코너가드 4개입 | 상품명(브랜드명 포함 가능) |
| `price` | ✅ | 12900 | 숫자(원) |
| `image` | ✅ | https://… | 대표 이미지 URL |
| `url` | ✅ | https://… | 상품 상세 URL |
| `rating` | △ | 4.8 | 평점 |
| `reviewsCount` | △ | 1284 | 리뷰 수 |

> **연령대(월령)는 아가드몰의 "월령별 추천" 카테고리에서 자동 매핑됩니다.**
> 카페24 API로 카테고리(예: `1~3개월`, `4~6개월` …)를 읽어 상품의 ageGroup을 자동 부여 → **별도 엑셀 불필요.**
> 시스템 연령대 체계(`AGE_GROUPS`)는 현재: `출산준비물, 1~3개월, 4~6개월, 7~9개월, 10~12개월, 13~18개월, 19~24개월, 24개월~`
> (13개월 이상 구간은 추정 — 실제 카페24 카테고리명으로 확정 필요)
> **상품명 안의 브랜드명**으로도 브랜드를 보정 매칭할 수 있습니다(brand 컬럼이 비어도 대비).

---

## 2. 인스타그램 피드 자동화

공개 피드 **스크래핑은 인스타 정책 위반·차단** 위험 → 비권장. 공식 방법 3가지:

| 방식 | 자동화 | 필요한 것 | 비고 |
|---|:--:|---|---|
| **A. Instagram Graph API** (권장) | 🟢 완전 자동 | ① 인스타 *프로(비즈니스)* 계정 ② 연결된 페이스북 페이지 ③ Meta 개발자 앱 ④ 장기 액세스 토큰 | Actions cron으로 주기 호출 → `data/insta-*.json` 캐시 |
| **B. oEmbed 임베드** | 🟡 반자동 | 보여줄 게시물 URL 목록 | 원하는 게시물만 수동 선택해 임베드 |
| **C. 수동 export** | 🔴 수동 | 이미지/캡션 직접 입력 | 가장 단순, 지금의 placeholder 방식 |

- 현재는 **placeholder**(실제 계정으로 링크되는 6칸)로 동작 → 계정 연결 전에도 안 깨짐.
- A 선택 시: 토큰은 **GitHub Actions Secrets**에 저장, cron(예: 1일 1회)으로 갱신.

---

## 3. 진행 중 행사/프로모션 자동화

| 소스 | 용도 | 자동화 방법 |
|---|---|---|
| **카페24 Admin API** | 진열/혜택/쿠폰/메인배너 | 개발자센터 앱 등록 → OAuth 토큰 → 상품·혜택 조회. Actions cron → `data/promotions.json` |
| **사방넷(Sabangnet) API** | 주문·재고·**송장(운송장) 실연동** | 배송조회 실데이터화에 적합(아래 4번) |

- 권장 최소 구성: **GitHub Actions cron(예: 1시간)** → 카페24 API 호출 → `data/promotions.json` 커밋 → 프론트가 fetch.
- 데이터 모델은 `js/data.js`의 `PROMOTIONS = { aguard: [], babystandard: [] }` 에 이미 준비됨.

---

## 4. 운송장(배송조회) 실연동 — 선택

현재는 `TRACKING_DB` 목업입니다. 실연동 옵션:
- **사방넷**: 주문→송장번호 매핑이 있으므로, 주문번호로 송장/배송상태 조회 자동화 가능.
- **택배사/스마트택배(굿스플로) API**: 송장번호로 배송 단계 조회.
- 구조: 서버리스가 조회 → 프론트의 `track()` 가 `TRACKING_DB[no]` 대신 API 응답 사용.

---

## 권장 아키텍처 요약
```
[GitHub Actions cron / 서버리스]
   ├─ 카페24 API     → data/promotions.json, data/catalog.json
   ├─ Instagram API  → data/insta-aguard.json, data/insta-babystandard.json
   └─ 사방넷/택배 API → (선택) 배송 상태
                ↓ (커밋/배포)
[정적 프론트(GitHub Pages)]  ← JSON fetch만 수행 (키 노출 없음)
```

다음 단계는 위 중 **어떤 소스를 먼저 연동할지** 정해지면, 해당 어댑터(JSON 스키마 + fetch 코드)부터 구현합니다.
