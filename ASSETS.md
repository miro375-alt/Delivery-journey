# 🎨 배송 경로 시각화 — 필요 이미지/GIF 에셋 명세

> 컨셉: **마스코트 캐릭터가 "배송 여정 맵"의 경로를 따라 이동**하며, 현재 배송 단계에 맞는 위치/동작을 보여준다.
> 모든 에셋은 `assets/route/` 폴더에 아래 **파일명 그대로** 넣으면 코드가 자동으로 인식합니다.
> (파일이 없으면 emoji 플레이스홀더가 대신 표시됩니다 → 없어도 화면은 안 깨짐)

---

## 0. 공통 규격
- **배경 투명** (PNG / WebP / APNG / GIF 알파 채널)
- **2배 해상도(@2x)** 로 제작 → 레티나에서도 선명
- 권장 포맷 우선순위: **Lottie(JSON) > WebP/APNG > GIF** (용량·화질 유리, 단 GIF도 그대로 사용 가능)
- 캐릭터·아이콘은 **같은 그림체/톤**으로 통일 (선 굵기, 채도)
- 색상 톤: 메인 컬러 `#2f6bff`(파랑) 계열과 잘 어울리게

---

## 1. 마스코트 캐릭터 (경로를 따라 움직이는 주인공) — ⭐ 핵심
배송 단계(`data-status`)별로 동작이 다른 6종. 움직이는 GIF/APNG 권장.

| 파일명 | 단계 | 추천 동작/연출 |
|---|---|---|
| `assets/route/char-ordered.gif`   | 주문접수 | 주문서 확인 / 손 흔들며 인사 |
| `assets/route/char-prepared.gif`  | 상품준비 | 박스에 상품 담아 포장 |
| `assets/route/char-picked.gif`    | 집화완료 | 박스 들고 출발 준비 / 트럭에 싣기 |
| `assets/route/char-transit.gif`   | 간선이동 | **트럭 운전(가장 '이동'스러운 연출)** |
| `assets/route/char-delivery.gif`  | 배송중   | 오토바이/도보로 배달 |
| `assets/route/char-delivered.gif` | 배송완료 | 집 앞 도착 / 박스 전달·세리머니(반짝이) |

- **크기**: 480×480px (@2x, 화면 표시 약 160~200px)
- **루프**: 무한 반복 (단, `char-delivered`는 1회 재생 후 정지여도 OK)
- **프레임**: 12~15fps 정도면 충분
- **용량**: 가능하면 각 500KB 이하 (GIF는 색상 256 제한 고려)

> 6종 다 만들기 부담되면 **최소 2종**(`char-transit` 트럭, `char-delivery` 배달)만 먼저 주셔도 됩니다.
> 나머지 단계는 그동안 emoji/정지 이미지로 대체합니다.

---

## 2. 경유지 노드 아이콘 (경로 위 정류장) — 정지 이미지
경로에 박히는 5개 지점. 정지 PNG/WebP면 충분(움직임 불필요).

| 파일명 | 지점 | 추천 일러스트 |
|---|---|---|
| `assets/route/node-warehouse.png` | 출발 · 물류센터 | 창고 건물 |
| `assets/route/node-hub.png`       | 허브 (간선상차) | 큰 분류센터 |
| `assets/route/node-terminal.png`  | 터미널 (간선하차) | 지역 터미널 건물 |
| `assets/route/node-branch.png`    | 배송지점 | 동네 영업소/트럭 |
| `assets/route/node-home.png`      | 도착 · 집 | 집/현관 |

- **크기**: 240×240px (@2x, 표시 약 80~120px), 정사각
- **상태 구분 불필요**: "아직 안 지난 지점"은 코드에서 흐리게(회색/반투명) 자동 처리합니다 → **1개 버전만** 제작
- 5개 그림체 통일

---

## 3. (선택) 경로 배경 — 길/지도 느낌
직접 안 만드셔도 됩니다(없으면 CSS로 점선 경로를 그립니다). 손그림 느낌의 길을 원하시면:

| 파일명 | 용도 | 규격 |
|---|---|---|
| `assets/route/path-bg.png` | 경로 배경(길/지도) | 가로로 늘어나는 형태, 1600×320px, 좌우 이어 붙여도 자연스럽게(seamless) |

---

## 4. (선택) 현재 위치 말풍선/핀
없으면 CSS로 처리합니다. 원하시면:

| 파일명 | 용도 | 규격 |
|---|---|---|
| `assets/route/pin.png` | "현재 여기" 핀/말풍선 | 120×140px, 투명 |

---

## 단계 ↔ 경로 위치 매핑 (코드가 이렇게 배치합니다)
```
[물류센터]───[허브]───[터미널]───[배송지점]───[집]
   ▲ordered      ▲transit            ▲delivery   ▲delivered
   ▲prepared
        ▲picked(출발)
```
- ordered / prepared → 물류센터에 캐릭터 위치
- picked → 물류센터 출발(물류센터~허브 사이)
- transit → 허브~터미널 구간 이동(트럭)
- delivery → 배송지점~집 구간(배달)
- delivered → 집 도착

---

## 제출 방법
1. `assets/route/` 폴더에 위 파일명대로 넣기
2. 브랜치에 커밋/푸시 (또는 저에게 주시면 배치)
3. 자동 배포로 사이트에 바로 반영

**우선순위 추천**: 1번(캐릭터 GIF) > 2번(노드 아이콘) > 나머지는 선택.
GIF가 어렵다면 정지 PNG 한 장씩만 주셔도 컨셉은 충분히 살아납니다.
